<aside>
  <%= link_to t('docs.return_docs'), docs_path, class: 'link-return' %>

  <h2>交互</h2>

  <ul>
    <% %w(custom_elements api).each do |section| %>
    <li>
      <%= link_to t(".#{section}"), doc_path(path: "interaction/#{section}"), class: @doc_path == "interaction/#{section}" ? 'active' : '' %>
    </li>
    <% end %>
  </ul>
</aside>
