<%= render 'docs/form/sidebar' %>

<% person = Doc::Form::Person.new %>

<article>
  <h3>用法</h3>

  <p>Tao Form 提供了 checkbox 和 switch 两种不同交互的布尔字段：</p>

  <div class="example">

<pre><code class="erb"><%= %Q(\
\<%= tao_form person, url: docs_persons_path, remote: true do |f| %\>
  \<%= f.input :married, as: :boolean, input_html: {checked: true} %\>
  \<%= f.input :handsome, as: :boolean, switch: true %\>
\<% end %\>
) %></code></pre>

    <div class="demo<%= ' without-padding' if request.variant.mobile? %>">
      <%= tao_form person, url: docs_persons_path, remote: true do |f| %>
        <%= f.input :married, as: :boolean, input_html: {checked: true} %>
        <%= f.input :handsome, as: :boolean, switch: true %>
      <% end %>
    </div>
  </div>

  <h3>组件</h3>

  <p>checkbox 和 switch可以脱离 input 作为单独的字段渲染，例如</p>

  <div class="example">

<pre><code class="erb"><%= %Q(\
\<%= tao_form person, url: docs_persons_path, remote: true do |f| %\>
  \<%= tao_check_box f, :married, disabled: true, class: 'date-field' %\>
\<% end %\>
) %></code></pre>

    <div class="demo<%= ' without-padding' if request.variant.mobile? %>">
      <%= tao_form person, url: docs_persons_path, remote: true do |f| %>
        <%= tao_check_box f, :married, disabled: true, class: 'date-field' %>
      <% end %>
    </div>
  </div>

  <p>也可以脱离表单单独使用的，例如：</p>

  <div class="example">

<pre><code class="erb"><%= %Q(\
\<%= tao_switch do %\>
  \<%= check_box_tag :handsome, '1', true %\>
\<% end %\>
) %></code></pre>

    <div class="demo">
      <%= tao_switch do %>
        <%= check_box_tag :handsome, '1', true %>
      <% end %>
    </div>
  </div>

  <h3>初始化选项</h3>

  <%= tao_api_item name: 'disabled', tags: ['Boolean'] do %>
    <p>指定组件的禁用状态。</p>
  <% end %>

  <%= tao_api_item name: 'checked_value', tags: ['String', 'Default: "1"'] do %>
    <p>指定字段选中状态的 value。</p>
  <% end %>

  <%= tao_api_item name: 'unchecked_value', tags: ['String', 'Default: "0"'] do %>
    <p>指定字段未选中状态的 value。</p>
  <% end %>

  <h3>公共属性</h3>

  <%= tao_api_item name: 'checked', tags: ['Boolean'] do %>
    <p>获取或者设置组件的选中状态。</p>
  <% end %>

  <h3>事件</h3>

  <%= tao_api_item name: 'tao-check-box:change / tao-radio-button:change / tao-switch:change', params: ['option'] do %>
    <p>当组件的选中状态发生变化时触发。</p>
  <% end %>

</article>
