<% if @person.errors.any? && params[:form_tpl].present? %>
<% form_selector = params[:form_selector].presence || '.person-form'%>
$('<%= form_selector %>').replaceWith('<%= j render params[:form_tpl], person: @person, narrow: params[:narrow].present? %>')
<% else %>
// do noting
<% end %>
