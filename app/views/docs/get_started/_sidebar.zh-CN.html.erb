<aside>
  <%= link_to t('docs.return_docs'), docs_path, class: 'link-return' %>

  <h2>开始使用</h2>

  <ul>
    <% %w(install init_details file_structure).each do |section| %>
    <li>
      <%= link_to t(".#{section}"), doc_path(path: "get_started/#{section}"), class: @doc_path == "get_started/#{section}" ? 'active' : '' %>
    </li>
    <% end %>
  </ul>
</aside>
