<aside>
  <%= link_to t('docs.return_docs'), docs_path, class: 'link-return' %>

  <h2>Tao Editor</h2>

  <ul>
    <% sections = %w(demo) %>
    <% sections.each do |section| %>
    <li>
      <%= link_to t(".#{section}"), doc_path(path: "editor/#{section}"), class: @doc_path == "editor/#{section}" ? 'active' : '' %>
    </li>
    <% end %>
  </ul>
</aside>
