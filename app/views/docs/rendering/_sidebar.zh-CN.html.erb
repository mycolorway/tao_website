<aside>
  <%= link_to t('docs.return_docs'), docs_path, class: 'link-return' %>

  <h2>渲染</h2>

  <ul>
    <% %w(get_started component api).each do |section| %>
    <li>
      <%= link_to t(".#{section}"), doc_path(path: "rendering/#{section}"), class: @doc_path == "rendering/#{section}" ? 'active' : '' %>
    </li>
    <% end %>
  </ul>
</aside>
