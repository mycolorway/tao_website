<aside>
  <%= link_to t('.return_docs'), docs_path, class: 'link-return' %>

  <h2>Tao UI</h2>

  <ul>
    <% %w(basic icons buttons customization).each do |section| %>
    <li><%= link_to t(".#{section}"), doc_path(path: "ui/#{section}"), class: @doc_path == "ui/#{section}" ? 'active' : '' %></li>
    <% end %>
  </ul>
</aside>
